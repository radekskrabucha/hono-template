{
  "name": "hono-open-api-template",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "build": "tsc",
    "test": "NODE_ENV=test vitest",
    "format": "prettier ./src --write",
    "lint": "eslint \"src/**/*.+(ts|js|tsx|jsx)\"",
    "lint:fix": "eslint \"src/**/*.+(ts|js|tsx|jsx)\" --fix",
    "type-check": "tsc --noEmit",
    "db:migrate": "drizzle-kit migrate",
    "db:generate-migration": "drizzle-kit generate",
    "db:setup": "drizzle-kit generate && drizzle-kit migrate",
    "docker:build": "docker build -t hono-openapi-app .",
    "docker:run": "docker run -p ${PORT:-4000}:${PORT:-4000} -e DATABASE_URL=$DATABASE_URL -e NODE_ENV=${NODE_ENV:-production} -e LOG_LEVEL=${LOG_LEVEL:-info} -e PORT=${PORT:-4000} hono-openapi-app",
    "docker:compose": "docker-compose up --build"
  },
  "dependencies": {
    "@hono/node-server": "1.13.2",
    "@hono/swagger-ui": "0.4.1",
    "@hono/zod-openapi": "0.16.4",
    "@neondatabase/serverless": "0.10.1",
    "dotenv": "16.4.5",
    "drizzle-orm": "0.35.1",
    "drizzle-zod": "0.5.1",
    "hono": "4.6.5",
    "hono-pino": "0.3.0",
    "pino": "9.5.0",
    "pino-pretty": "11.3.0",
    "typescript": "5.6.3",
    "zod": "3.23.8"
  },
  "devDependencies": {
    "@total-typescript/tsconfig": "1.0.4",
    "@trivago/prettier-plugin-sort-imports": "4.3.0",
    "@types/node": "22.7.5",
    "drizzle-kit": "0.26.2",
    "eslint": "9.12.0",
    "eslint-config-prettier": "9.1.0",
    "globals": "15.11.0",
    "prettier": "3.3.3",
    "tsx": "4.19.1",
    "typescript-eslint": "8.9.0",
    "vitest": "2.1.3"
  }
}
